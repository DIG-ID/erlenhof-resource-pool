name: 🔁 Promote Old Jobs

on:
  schedule:
    - cron: "*/15 * * * *" # Corre a cada 15 minutos (ajusta se quiseres)
  workflow_dispatch: # Permite correr manualmente também

jobs:
  promote-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout do repositório
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Instalar dependências
        run: npm ci

      - name: 🔑 Criar ficheiro serviceAccount.json
        run: |
          mkdir -p functions
          echo '${{ secrets.SERVICE_ACCOUNT_KEY }}' > functions/serviceAccount.json

      - name: ▶️ Executar promote-jobs.ts
        run: npx tsx functions/cron-promote-jobs.ts
