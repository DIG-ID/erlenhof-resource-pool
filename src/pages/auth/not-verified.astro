---
import Layout from "@/layouts/Layout.astro";
import { AlertCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
---

<Layout title="Account Not Verified">
  <section class="flex flex-col items-center justify-center gap-6 py-24 text-center px-4">
    <div class="flex flex-col items-center gap-2">
      <AlertCircle size={48} className="text-yellow-500" />
      <h1 class="text-2xl font-bold text-yellow-600">Account not yet active</h1>
      <p class="max-w-md text-muted-foreground">
        Your account was created successfully, but it still needs to be activated by an administrator.
        Once approved, you’ll be able to log in and access the platform.
      </p>
    </div>
    <Button>
      <a href="/auth/login">Back to Login</a>
    </Button>
  </section>
</Layout>